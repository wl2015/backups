<#macro message text="" count="" user=user name=user.userName!"paul">
	<#-- 判断msg是否存在 -->
	<@s.mt code=label text=label/>
	<style>
	.demo{
		width:${width}px;
		height:${height}px;
		border: 1px green solid;
		background-color: gray;
		marin:${left}px auto
	}
	</style>
	<#if text!="">
		<div class="demo">
			${text}.${name}.${count}
		</div>
	</#if>
	
</#macro>